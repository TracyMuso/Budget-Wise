<div class="w-full content-center p-2">
  <header class="flex flex-row p-4 w-full items-center space-x-4">
     <div class="mr-4 p-2 ml-8">
       <span>
         <p class="p-2">
           <i class="fa-regular fa-user"></i>
           <%= current_user.name %>
         </p>
       </span>
     </div>
     <h1 class="font-bold p-2 text-3xl ml-4">CATEGORIES</h1>
     <div class="text-center mx-2">
     <%= button_to 'Log out', destroy_user_session_path,  method: :delete, class: ' btn rounded-lg bg-gray-500 w-75 p-3 mt-2 text-white' %><br>
   </div>
  </header>
  <div class="d-flex content-center mx-auto my-4 text-center mb-6">
   <% if notice.present? %>
  <p class="py-2 px-3 bg-green-50 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>
    <em>Add new categories of the expenses you wanna make, eg Groceries</em>
</div>
  <div class="d-flex justify-between w-2/3 ml-6 items-center p-4">
    <div class="my-4 p-2 mx-auto w-1/2">
      <%= link_to 'Add New Category', new_group_path, class: " btn rounded-lg py-3 px-5 bg-blue-600 text-white font-medium" %>
    </div>

    <% if current_user.groups.empty? %>
      <p class="text-black my-6 mx-auto">You have no categories yet</p>
    <% else %>
      <p class="text-gray-500">You have <%= current_user.groups.count %> categories</p>
      <div class="cats mx-auto my-4 p-4">
       <% current_user.groups.each do |group| %>
         <%= link_to group_path(group), class: 'no-underline' do %>
           <div class="flex bg-white border-gray-200 rounded-lg shadow-md p-4 mb-4 mt-4 mx-auto my-hover:     bg-gray-700 items-center space-x-4">
              <div class="w-6 h-6 mx-4">
                <%=image_tag(group.icon, alt: 'icon', class: 'img')%>
             </div>
             <h1 class="font-bold text-2xl mx-4 p-2"><%= group.name %></h1>
             <div class="p-2 mx-4 flex-end">
               <% total = 0.00 %>
               <% group.group_categories.each {|group_categories| total += group_categories.category.amount} %>
               <span class="amount p-2">Total: $<%= total %></span>
             </div>
            </div>
             <% end %>
          <% end %>
    <% end %>
  </div><br><br>
</div>
